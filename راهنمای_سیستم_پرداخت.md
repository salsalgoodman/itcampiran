# راهنمای سیستم پرداخت

## خلاصه تغییرات

سیستم پرداخت کارت به کارت به ربات اضافه شد:

### ساختار پرداخت:
- **درس 1 و 2**: رایگان 🆓
- **درس 3 به بعد**: نیاز به پرداخت 💰 (100,000 تومان)

### اطلاعات کارت:
- **شماره کارت**: 6063731112299749
- **به نام**: سیرجان نژاد

### فرآیند پرداخت:

1. **کاربر درس 3+ را انتخاب می‌کند**
   - ربات پیام پرداخت را نمایش می‌دهد
   - اطلاعات کارت نمایش داده می‌شود

2. **کاربر فیش واریزی را ارسال می‌کند**
   - عکس فیش در Supabase Storage ذخیره می‌شود
   - وضعیت کاربر به "pending" تغییر می‌کند

3. **ادمین تایید می‌کند**
   - ادمین (5813277691) پیام را دریافت می‌کند
   - دکمه‌های "تایید" و "رد" نمایش داده می‌شود
   - بعد از تایید، دسترسی کاربر فعال می‌شود

## فیلدهای دیتابیس مورد نیاز

در جدول `users` باید این فیلدها وجود داشته باشند:

```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS payment_method TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS payment_amount INTEGER;
ALTER TABLE users ADD COLUMN IF NOT EXISTS payment_receipt_url TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS has_paid_for_lessons BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS payment_status TEXT DEFAULT 'pending';
ALTER TABLE users ADD COLUMN IF NOT EXISTS payment_approved_at TIMESTAMPTZ;
```

## Storage Bucket

برای ذخیره فیش‌های واریزی، باید یک bucket به نام `receipts` در Supabase Storage ایجاد کنید:

1. به Supabase Dashboard بروید
2. Storage را باز کنید
3. "New bucket" را بزنید
4. نام: `receipts`
5. Public: No (برای امنیت)
6. Create

## تنظیمات Environment Variables

در Railway یا `.env`:

```
ADMIN_IDS=5813277691
```

## تست سیستم

1. با یک کاربر تست، درس 3 را انتخاب کنید
2. پیام پرداخت باید نمایش داده شود
3. یک عکس ارسال کنید (فیش واریزی)
4. ادمین باید پیام تایید را دریافت کند
5. بعد از تایید، کاربر باید بتواند به درس‌ها دسترسی داشته باشد

## نکات مهم

- اگر Storage در دسترس نباشد، سیستم بدون ذخیره فیش ادامه می‌دهد
- ادمین می‌تواند پرداخت را تایید یا رد کند
- بعد از رد، کاربر می‌تواند دوباره فیش ارسال کند
- بعد از تایید، `has_paid_for_lessons` به `True` تغییر می‌کند

