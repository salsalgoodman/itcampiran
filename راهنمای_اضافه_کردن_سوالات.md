# راهنمای اضافه کردن سوالات برای درس‌های ۹ به بعد

## مشکل
درس‌های ۹ به بعد سوالات آزمون ندارند و کاربران نمی‌توانند به درس بعدی بروند.

## راه حل

### روش 1: استفاده از فایل SQL (توصیه می‌شود)

1. به **Supabase Dashboard** بروید
2. در منوی سمت چپ، **SQL Editor** را انتخاب کنید
3. فایل `add_questions_9_15.sql` را باز کنید
4. محتوای فایل را کپی کنید
5. در SQL Editor، محتوا را paste کنید
6. روی دکمه **Run** کلیک کنید
7. باید پیام موفقیت را ببینید و تعداد سوالات برای هر درس نمایش داده شود

### روش 2: استفاده از اسکریپت Python

اگر دسترسی به اینترنت مستقیم دارید (بدون فیلتر):

```bash
python add_missing_questions.py
```

این اسکریپت به صورت خودکار:
- درس‌های ۹ تا ۱۵ را پیدا می‌کند
- سوالات موجود را بررسی می‌کند
- سوالات گمشده را اضافه می‌کند

## بررسی نتیجه

بعد از اجرای SQL یا اسکریپت، می‌توانید بررسی کنید:

```sql
SELECT 
    l.lesson_number,
    l.title,
    COUNT(q.id) as question_count
FROM lessons l
LEFT JOIN questions q ON q.lesson_id = l.id
WHERE l.lesson_number >= 9
GROUP BY l.lesson_number, l.title
ORDER BY l.lesson_number;
```

باید برای هر درس حداقل ۱ سوال وجود داشته باشد.

## سوالات اضافه شده

- **درس 9**: 1 سوال (مدیریت خطا)
- **درس 10**: 1 سوال (ماژول‌ها)
- **درس 11**: 1 سوال (برنامه‌نویسی شی‌گرا)
- **درس 12**: 1 سوال (مقدمه ربات تلگرام)
- **درس 13**: 1 سوال (دستورات و پیام‌ها)
- **درس 14**: 1 سوال (دکمه‌ها و منو)
- **درس 15**: 1 سوال (ربات کامل با دیتابیس)

## نکات مهم

- اگر سوالی از قبل وجود داشته باشد، به‌روزرسانی می‌شود (ON CONFLICT)
- همه سوالات با `question_number = 1` اضافه می‌شوند
- برای اضافه کردن سوالات بیشتر، می‌توانید فایل SQL را ویرایش کنید

